<form method="post" action="{{ path('author_delete', {id: author.id}) }}" onsubmit="return confirm('Delete?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button>Delete</button>
</form>