{% extends 'base.html.twig' %}
{% block title %}New Author{% endblock %}
{% block body %}
    <h1>Create Author</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Save</button>
    {{ form_end(form) }}
    <a href="{{ path('author_index') }}">back to list</a>
{% endblock %}