<form method="post" action="{{ path('issue_delete', {id: issue.id}) }}" onsubmit="return confirm('Delete?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ issue.id) }}">
    <button>Delete</button>
</form>