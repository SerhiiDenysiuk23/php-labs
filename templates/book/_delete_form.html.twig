<form method="post" action="{{ path('book_delete', {id: book.id}) }}" onsubmit="return confirm('Delete?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
    <button>Delete</button>
</form>