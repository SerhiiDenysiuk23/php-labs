{% extends 'base.html.twig' %}
{% block title %}New Book{% endblock %}
{% block body %}
    <h1>Create Book</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Save</button>
    {{ form_end(form) }}
    <a href="{{ path('book_index') }}">back to list</a>
{% endblock %}