{% extends 'base.html.twig' %}
{% block title %}Edit Return{% endblock %}
{% block body %}
    <h1>Edit Return</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Update</button>
    {{ form_end(form) }}
    <a href="{{ path('return_index') }}">back to list</a>
{% endblock %}