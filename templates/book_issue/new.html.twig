{% extends 'base.html.twig' %}
{% block title %}New Issue{% endblock %}
{% block body %}
    <h1>Create Issue</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Save</button>
    {{ form_end(form) }}
    <a href="{{ path('issue_index') }}">back to list</a>
{% endblock %}